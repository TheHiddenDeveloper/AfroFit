import 'package:flutter/material.dart';

class BeginnerWidget extends StatelessWidget {
  const BeginnerWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        "Beginner Workout Plan",
        style: TextStyle(fontSize: 24),
      ),
    );
  }
}
